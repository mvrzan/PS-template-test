{
  "task_routing": {
    "filters": [
      {
        "expression": "1==1",
        "targets": [
          {
            "queue": "${QUEUE_SID_PROFS}",
            "expression": "task.targetWorker==worker.contact_uri"
          }
        ]
      }
    ]
  }
}
